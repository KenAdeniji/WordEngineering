#!/bin/bash
chmod +x filename #2021-10-12 Change the mode of the file to executable.
who | wc -l #2021-10-12 Count the number of users logged in.
variable=1 #Set variable
echo $variable #Display the value of a variable
grep 'pattern' filename #Search for the occurrence of the pattern, in the filename.
