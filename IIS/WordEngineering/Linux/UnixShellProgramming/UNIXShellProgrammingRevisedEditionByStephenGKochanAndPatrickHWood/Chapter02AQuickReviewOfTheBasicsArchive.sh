#!/bin/bash
date #2021-10-11 Print the system date and time.
who #2021-10-11 List all the users who are currently logged into the system.
who am i #The current user.
echo hello world. #Re-print the information typed.
ls #List the filenames
cat <filename> #Display the content of this filename
wc <filename> #Display the number of lines, words and characters
cp <existingPathname> <targetPathname> #Copy the content of the source.
mv <formerFilename> <newPathname> #Rename the previous name.
rm <existingPathname> #Remove an existing pathname.
cd <existingDirectoryname> #Change to an existing directory
mkdir <newDirectoryname> #Create a new directory
